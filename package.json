{
  "name": "task",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "back": "cd backend && npm install && npx prisma generate && npm run dev",
    "front": "cd ../frontend && npm install && npm run dev",
    "install:backend": "cd backend && npm install && npx prisma generate",
    "install:frontend": "cd frontend && npm install",
    "dev": "npm run install:backend && npm run install:frontend && npm run dev",
    "start": "npm run dev"
  },  
  "author": "",
  "license": "ISC"
}
